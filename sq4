import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestRegressor
from sklearn.metrics import mean_squared_error
import matplotlib.pyplot as plt

# Step 1: Data Collection (Assuming you have collected data from different social platforms)
# Example DataFrame with dummy data
data = {
    'text_length': [120, 80, 150, 100, 90],
    'image_quality': [8, 6, 9, 7, 8],
    'post_type': ['text', 'image', 'text', 'image', 'text'],
    'day_of_week': [3, 4, 1, 5, 2],
    'hour_of_day': [10, 15, 8, 18, 12],
    'likes': [200, 150, 300, 250, 180]  # Target variable
}
df = pd.DataFrame(data)

# Step 2: Data Preprocessing (No preprocessing required for this example)

# Step 3: Feature Engineering
# Separate features (X) and target variable (y)
X = df.drop(columns=['likes'])
y = df['likes']
